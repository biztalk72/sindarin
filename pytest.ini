[pytest]
# asyncio auto mode: async test functions run without an explicit marker.
asyncio_mode = auto
testpaths = tests apps/api/tests
addopts = -q
markers =
    eval: golden-query / citation eval regression (release-gated)
    integration: requires postgres / vector-db / object-store
    e2e: full upload→ingest→chat smoke path
