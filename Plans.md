# Plans — Hybrid IDP work ledger

> Source of truth for in-flight work. Generated from PRD2 §12.3 (epics), §14.1 (release
> phases), §15 (ADRs). Markers: `cc:TODO` (not started) · `cc:WIP` (in progress) ·
> `cc:完了` (done). Procedure: CLAUDE.md §6 — pick the top `cc:TODO`, mark `cc:WIP`,
> implement + test, mark `cc:完了`. Requirement IDs and acceptance criteria are from PRD2.

## Release phases (PRD2 §14.1)

| Phase | Goal | Epics | Done when |
|---|---|---|---|
| **0 / MVP** | Operable minimal RAG | E1, E2, E3 (DOCX), E4 (PDF OCR), E5 (HWPX), E6, E7, E9 (doc icon view) | 4 sample docs end-to-end |
| **1 / Beta** | Format breadth + UI insight | E3 (XLSX/PPTX/HTML/CSV/JSON/XML), E10 | select → keyword click → ask path |
| **2** | Eval + admin ops | E8, E11 | nightly eval + admin health dashboard |
| **3** | GB10 optimization | (E1/E6 perf) worker scheduling, caching, batching, telemetry, backup/restore | p95 latency + restore drill met |
| **4** | Scale options | OIDC/SAML, cold standby, external object/vector store, GitOps | applied per org need |

---

## E1 — Foundation (INFRA-001) · Phase 0
*Done when: local compose runs API, UI, Postgres.*

- `cc:完了` Monorepo scaffold — apps/workers/packages/infra/tests/docs (ADR-0010) — *scaffolded 2026-06-05*
- `cc:完了` Shared config — root `pyproject.toml` (uv workspace), `pytest.ini`, `Makefile`, `env.example`
- `cc:完了` Docker base images authored — `infra/docker/{api,web,worker}.Dockerfile`
- `cc:完了` Compose topology authored — `infra/compose/{dev,test}.yml` (api, web, postgres, MinIO, Qdrant, 5 workers)
- `cc:完了` **Verify `make up`** — dev stack up & verified 2026-06-05: postgres healthy, api `/api/health`→200, web→200, qdrant ready, minio live. Fixes applied: generated `uv.lock`; api.Dockerfile now syncs/runs `--package hybrid-idp-api` (was running the workspace-root env → uvicorn missing) and copies `workers/` for workspace completeness; web/api host ports configurable (`WEB_PORT`/`API_PORT`) — local :3000 was taken by an unrelated app, verified web on :3001.
- `cc:完了` Verify clean install from scratch — proven via container builds: api image `uv sync --frozen` from lockfile (no dev), web image clean `npm install` + `next build` both succeed
- `cc:TODO` Build the 5 worker stub images (`docker compose build worker-*`) — skipped during INFRA-001 verify (stubs `sleep infinity`); validate once a worker has a real entrypoint

## E2 — Document IR (IR-001) · Phase 0
*Done when: MarkItDown/OCR/HWPX outputs all pass the same schema.*

- `cc:完了` IR schema — `packages/document_ir` (PRD2 §6.1: Block, BBox, TableSchema, SemanticKeyword, Quality, DocumentIR)
- `cc:完了` Vector payload contract — `packages/rag_core` `ChunkPayload` (PRD2 §6.3)
- `cc:完了` Cross-parser validator + golden fixtures — `document_ir.validate_ir` (schema + invariants: citation anchor present/1-based, unique block_ids, TABLE→table_schema, OCR-005 confidence gate); 4 golden IR fixtures (markitdown docx/xlsx, paddleocr-vl pdf, hwpx-native) under `tests/fixtures/ir/`; 16 tests green. *Done 2026-06-05. Workers must call `validate_ir` before emitting (the shared E2 gate).*
- `cc:完了` Postgres relational model + migrations — `packages/db` (SQLAlchemy 2.0, 12 tables incl. `users` for FKs); Alembic wired (`alembic.ini`, `infra/migrations/env.py`, `make migrate`/`make migration`); initial revision autogenerated + applied + down/up roundtrip verified against running pg (13 tables incl. `alembic_version`). Also fixed compose cred consistency: COMPOSE now uses `--env-file .env` (interpolation dir ≠ -f path) so postgres init creds match the api's `env_file`; added `POSTGRES_PUBLISHED_PORT` (host :5433). *Done 2026-06-05. Closes E2 / IR-001.*

## E3 — MarkItDown (MD-001..005) · Phase 0 (DOCX) → Phase 1 (rest)
*Done when: Office sample conversion + snapshot test pass.*

- `cc:完了` MD-001 MarkItDown adapter — `markitdown_worker.process()`; `MarkdownConverter` protocol + lazy `MarkItDownConverter` (injectable, ADR-0007)
- `cc:完了` MD-002 Markdown block IDs — `mapper.markdown_to_blocks` emits heading/paragraph/list/table blocks with `b{n}` ids + nested `section_path`
- `cc:完了` MD-003 Table JSON preservation — GFM tables → `TableSchema` (headers + rows)
- `cc:完了` MD-002b Synthetic pagination — 1-based `page_no` incremented at shallowest heading level (ADR-0007); verified DOCX H1 + XLSX `##`-sheet cases
- `cc:完了` MD-004 Quality metrics — `extraction_coverage` + `table_preservation_score` added to `Quality`; warnings collected
- `cc:完了` MD-005 Fallback chain — primary→fallback converter retry, `used_fallback` flag + warning; reraises if no fallback. *9 tests; output passes `validate_ir`. Real-binary .docx e2e still deferred (ADR-0007).*

## E4 — OCR/PDF (OCR-001..006) · Phase 0
*Done when: scanned PDF fixture produces page OCR output.*

- `cc:完了` OCR-001 PDF classifier — `classify_from_page_texts` (text-layer ratio → digital/scanned/hybrid) + `classify_pdf` pypdf glue; injectable extractor
- `cc:TODO` OCR-002 Page render pipeline — image, DPI, rotation, deskew, denoise *(env-blocked: needs PDF rendering libs; behind `OcrEngine` protocol)*
- `cc:TODO` OCR-003 PaddleOCR text extraction — per-page text, bbox, confidence *(env-blocked: PaddleOCR models; `PaddleOcrEngine.recognize` raises until wired; fake engine tested)*
- `cc:TODO` OCR-004 PaddleOCR-VL layout extraction — tables, charts, formulas (ADR-0009) *(env-blocked: VL model)*
- `cc:完了` OCR-005 Quality gate — `ir_from_ocr` flags mean confidence < 0.90 as `needs_reprocess` warning
- `cc:完了` OCR-006 Citation anchor — digital + OCR paths emit blocks carrying page_no + bbox + block_id; output passes `validate_ir`. *8 tests; digital path fully runnable, scanned path tested via injected engine.*

## E5 — HWPX (HWPX-001..006) · Phase 0
*Done when: HWPX fixture yields TOC + keywords.*

- `cc:完了` HWPX-001 Package parser — `package.open_package` reads OWPML zip (header + section*.xml + .hpf) by suffix; namespace-agnostic XML parse (local-name matching)
- `cc:完了` HWPX-002 Style-based TOC — outline levels from header `paraPr` → heading blocks + nested `section_path` + synthetic pagination at shallowest level
- `cc:完了` HWPX-003 Table extractor — `<tbl>/<tr>/<tc>` → `TableSchema` (headers + rows)
- `cc:完了` HWPX-004 Metadata extractor — DC fields from `content.hpf` (author/date/title) + security_label candidate
- `cc:完了` HWPX-005 Semantic keyword extractor — regex for 법령명/제N조/기관명/날짜/금액 with frequency weights
- `cc:完了` HWPX-006 Fallback path — `HwpxPackageError` → optional render/OCR `fallback` hook, `used_fallback` flag; reraises if none. *9 tests against a real in-zip OWPML fixture; output passes `validate_ir`.*

## E6 — Indexing (IDX-001) · Phase 0
*Done when: payload-filterable vector index is created.*

- `cc:完了` Vector-DB backend — **provisional Qdrant** (dev, verified live); `QdrantVectorStore` + `InMemoryVectorStore` behind one `VectorStore` protocol. *Formal Qdrant-vs-pgvector benchmark ADR still deferred (PRD2 §15).*
- `cc:完了` Chunker — `rag_core.chunk_document`: table-isolated chunks, page-boundary splits, preserves toc_path/page_no/bbox (citation anchor)
- `cc:完了` Embedding worker — `embedding_worker.index`: batched embed w/ retry, versioned collection `base__vN`, `activate()` blue/green alias swap; injectable `Embedder` (`DeterministicEmbedder` for dev/tests, `OpenAIEmbedder` real via ADR-0001)
- `cc:完了` Vector writer — upserts full `ChunkPayload` (PRD2 §6.3); `payload_filter` ACL/metadata scope at search time (vector side of double-check); blue/green via aliases (ADR-0008)
- `cc:完了` BM25/keyword index — `rag_core.BM25Index` (pure-Python Okapi, Hangul-aware tokenizer) for exact-term/법령명 recall. *6 unit + 2 live-Qdrant integration tests. Embedding-model choice still a follow-up ADR.*

## E7 — RAG (RAG-001) · Phase 0
*Done when: answer generated with citations.*

- `cc:完了` Query understanding + scope resolution — `detect_language` (ko/en) + user `scope_document_ids` filter (`pipeline.answer`)
- `cc:完了` ACL filtering — vector `payload_filter` (security scope) + authoritative `Authorizer.allowed_documents` double-check (invariant #2); tested incl. confidential doc excluded
- `cc:完了` Hybrid retrieval — `hybrid_retrieve`: vector + BM25 fused via Reciprocal Rank Fusion; corpus hydration (text not in vector payload)
- `cc:完了` Reranking — RRF fusion is the v1 reranker. *Cross-encoder / LLM-judge reranker deferred (needs model endpoint).*
- `cc:完了` Context packing — `pack_context`: dedup + budget cap, relevance-order preserved (fixed a flaky doc/page re-sort bug)
- `cc:完了` `/api/chat` — `ChatRequest`/`ChatResponse` (PRD2 §9.1/§9.2), live endpoint gates honestly (503: endpoint-not-configured → docs-not-ingested), 200 cited answers w/ injected pipeline; citation validation drops uncited claims (ADR-0004). *SSE streaming deferred.*
- `cc:完了` Model router — `route_model` rule-based v1 (mode→model, hint wins). *LLM-judged routing deferred (PRD2 §15).*
- `cc:完了` **Real OpenAI-compatible generator + embedder** — `OpenAIGenerator` (claim-structured JSON, per-claim citations) + `OpenAIEmbedder` (batched), both injectable-client; `app.rag.build_pipeline` factory wires them + Qdrant from settings; `openai_configured` gate. *8 tests via faked transport incl. full pipeline end-to-end. Runtime needs a live endpoint + ingested corpus (E9).*

## E8 — Trust (TRUST-001) · Phase 2
*Done when: seeded hallucination + injection tests pass.*

- `cc:完了` Citation validator — done in E7 (`rag_core.trust.validate_citations`, drops uncited, ADR-0004)
- `cc:完了` Eval worker — `eval_worker.evaluate`: runs a golden set through the pipeline → Recall@10, citation-anchor accuracy, citation precision, unsupported-claim rate. *LLM-judge faithfulness + page-level anchor deferred.*
- `cc:完了` Guardrails — `rag_core.guardrails`: regex PII detect/redact (email/phone/RRN/사업자/card, EN+KO) + injection detect/strip, wired into the pipeline choke point (input scan, context injection-strip, output PII redaction → warnings + `guardrails` audit signal); **audit log** row per `/api/chat` (hashed payload, ADR-0006). Verified live (PII redacted, injection stripped, audit rows in Postgres). *Presidio NER + LLM-judge output filter are the documented upgrades.*
- `cc:完了` Prompt-injection — `strip_injection`/`detect_injection` (EN+KO signatures) remove injected instructions from retrieved context before generation; seeded pipeline test confirms the instruction never reaches the answer (PRD2 §7.1)
- `cc:完了` Release-gate eval wiring — `tests/eval/test_eval_gate.py` (marked `eval`) reads `thresholds.toml` and asserts the floors (Recall@10 ≥0.90, citation precision ≥0.90, unsupported ≤0.05); `make eval` runs it green

## Auth (ADR-0005) — single-org JWT — `cc:完了`
- `cc:完了` Backend — `app.auth`: HS256 JWT (sub+role, no tenant claim), PBKDF2 password hashing, `get_current_principal`/`require_roles` deps, env-driven bootstrap admin on startup; `POST /api/auth/login` + `GET /api/auth/me`; `users.password_hash` column + migration. Pipeline principals now come from the verified token (admin=unrestricted, else ACL by subject) — replaced the MVP admin default. Audit `actor_id` = token subject.
- `cc:完了` Frontend — token stored client-side + sent on every API call (via the `/api` proxy); `AuthGate` login form gates the workspace, validates the stored token via `/auth/me`, logout control.
- `cc:完了` Verified — 5 auth unit + 1 live login→me integration + 2 AuthGate Vitest tests; **live**: no-token→401, login→JWT, `/me`→admin, chat-with-token→cited answer, wrong-pw→401, bootstrap admin auto-created.
- `cc:完了` Enforcement on documents + upload — router-level auth deps on `/api/documents`, `/documents/{id}/{toc,keywords,graph,preview}`, `/ingest/jobs`, `/upload` (no token → 401); **ACL-filtered listing** (admin=all, else owned/granted) + per-document 404-on-unauthorized for insight/preview; upload sets `owner_id` from the token → owner ACL grant (invariant #2). 4 endpoint-auth unit tests + live verify (401 without token; 200 + ownership with token). *OIDC (ADR-0005 optional) + document_manager/auditor role scoping still deferred.*

## E9 — UI Core (UI-DOC-*, UI-CHAT-*) · Phase 0
*Done when: document-selection-based query works.*

- `cc:完了` Document Library — backend `GET /api/documents` + `GET /api/ingest/jobs/{id}`; `apps/web` `DocumentLibrary` pane: type-icon cards (UI-DOC-001), status badges (UI-DOC-002), chunk count, multi-select → chat scope (UI-DOC-004, verified live: scoped chat cites only the selected doc), drag-free upload (file input → `/api/upload` → refetch), empty state (UI-DOC-006). 3 Vitest tests + live verify via web proxy. *Deferred: full quality metrics on cards (UI-DOC-003, only chunk_count shown), card/list toggle + pagination (UI-DOC-005), ACL-filtered listing.*
- `cc:完了` **Upload center (backend)** — `POST /api/upload` routes by extension to the parser worker (markitdown/hwpx/ocr) → `app.ingest.ingest_ir` persists Document/Version/Blocks/Chunks/ACL/Job + indexes to the vector store. *UI + `GET /api/ingest/jobs/{id}` status-polling endpoint still TODO.*
- `cc:完了` Chat workspace — `apps/web` Next.js 15: 3-pane shell + live `ChatWorkspace` (messages, composer, suggested prompts UI-CHAT-001, citation chips UI-CHAT-002, groundedness/warnings UI-CHAT-004, error states, a11y labels). Same-origin `/api` proxy (next.config rewrites; build-time `API_PROXY_TARGET`). *Verified: tsc + 2 Vitest/RTL tests + `next build` + live browser→proxy→api→DB cited answer. Streaming (UI-CHAT-003) + theming (007) deferred.*
- `cc:TODO` i18n — next-intl EN/KO equal (invariant #5) *(labels are bilingual inline; next-intl not yet wired)*

### Live RAG wiring (E6/E7/E9 capstone) — `cc:完了`
- `cc:完了` `document_chunks` table + migration — persists chunk text so retrieval hydrates the corpus (vector payload omits text, PRD2 §6.3)
- `cc:完了` `app.repository` — `load_corpus`/`build_bm25` from Postgres + `PostgresAuthorizer` (acl_entries double-check; `admin`=unrestricted)
- `cc:完了` `app.rag.build_pipeline_from_db` + dev fallback — OpenAI embedder/generator when configured, else deterministic embed + extractive generate (offline-capable). `get_pipeline` builds the live pipeline; 503 if store down/empty
- `cc:完了` **Verified live**: `POST /api/upload` (CSV→MarkItDown→IR→persist+index) then `POST /api/chat` on the running container returns a grounded, citation-validated answer (groundedness 1.0, trace id). 2 live integration tests (Postgres+Qdrant) + 68 unit/api green. *Real-LLM synthesis lights up by setting OPENAI_API_KEY+EMBEDDING_MODEL — same path.*

## E10 — UI Insight (UI-VIZ) · Phase 1
*Done when: keyword click → section highlight works.*

- `cc:完了` TOC tree view — `GET /api/documents/{id}/toc` (nested from blocks' `section_path` + `seq`; persisted via new `document_blocks.seq/section_path` cols) + recursive `TocTree` UI w/ page numbers. Verified live (위약금 nests under 계약 조건). *Node-click→scope/highlight deferred (needs preview pane).*
- `cc:完了` Word cloud — `GET /api/documents/{id}/keywords` (frequency/doc-freq weighting + merged persisted HWPX semantic keywords) + weighted tag-cloud UI. *Full hybrid score (TF-IDF+BM25+embedding+style, PRD2 §8.3) deferred.*
- `cc:完了` Keyword graph — `GET /api/documents/{id}/graph` (co-occurrence, node cap 30 / edge cap 150) + inline-SVG circular graph (no dep). *Real graph lib (React Flow/Cytoscape/Sigma/ECharts) + entity graph + lazy-expand still an ADR follow-up.*
- `cc:完了` **Document preview pane** — `GET /api/documents/{id}/preview` (blocks in order: ref/page/type/text/section_path) + `PreviewPane` center overlay (UI-CHAT-005/006). **Citation chip click → preview + highlight cited span**; **TOC node click → preview + highlight section** — closes E10's "click → highlight" acceptance. Verified: 2 Vitest tests (span + section highlight) + chat citation-click test + live `/preview`. *Page images deferred (no renderer); highlight is text/section-based.*
- `cc:完了` Metadata + quality panel — `GET /api/documents/{id}/quality` (parser/coverage/ocr_confidence/warnings/blocks/chunks from the latest ingestion job, ACL-scoped) + InsightPanel **품질** tab (metrics grid, warnings, disabled reprocess button). Verified: live endpoint (401 no-token), 1 integration + 1 Vitest test. *Reprocess action (POST /ingest/jobs/{id}/retry) still TODO.*

## E11 — Admin/Ops (OPS-001) · Phase 2
*Done when: preprocessing failure cause + model latency are visible.*

- `cc:完了` Health dashboard — `GET /api/admin/health` (live postgres + Qdrant checks + model-endpoint status) + admin-only router; `/admin` page health card. Verified live (401 no-token, ok with admin).
- `cc:完了` Job logs + parser warnings — ingest now persists quality (parser/coverage/ocr_conf/warnings/blocks/chunks) to `ingestion_jobs.metrics`; `GET /api/admin/jobs` + dashboard table surface stage/status/warnings.
- `cc:完了` Metrics — `GET /api/admin/metrics` light collector (documents/chunks/users/audit counts, jobs-by-status, cpu_count) + `GET /api/admin/audit` recent log; dashboard cards. *Prometheus/Grafana export + real GB10 GPU/unified-memory/thermal telemetry deferred (collect on the node — surfaced as "unavailable").*
- `cc:完了` Admin UI + access — `apps/web/src/app/admin/page.tsx` + `AdminDashboard` (auth-gated; admin link in session bar); all `/api/admin/*` require admin role (403 for non-admin). 2 endpoint-auth + 3 live integration + 2 Vitest tests.
- `cc:TODO` Admin override flow — guardrail/ACL bypass with reason + audit + optional 2-person approval (PRD2 §10.1) — distinct write-path feature, deferred.

## E12 — CI/CD (CICD-001) · Phase 0→2
*Done when: dev deploy + smoke test automated.*

- `cc:完了` Harness pipeline — `infra/harness/pipeline.yml`: full **test → build/push → deploy-staging → approval → deploy-prod** with smoke + eval gate + `HarnessApproval` + `StageRollback` (valid YAML, 5 stages). Account/connector/registry are clearly-marked `<PLACEHOLDER>`s to fill per deployment.
- `cc:完了` security-scan stage — `make security-scan` (pip-audit + npm audit + gitleaks, best-effort) + pipeline step (PRD2 §13.1)
- `cc:完了` smoke-tests — `tests/e2e/smoke/test_smoke.py` (health→auth→login→documents→chat) + `infra/scripts/smoke.sh`; verified live against dev **and** a real staging deployment
- `cc:完了` eval-gate stage — `make eval` (thresholds.toml) wired into deploy-staging before prod promotion
- `cc:完了` prod approval → deploy → continuous-verification → rollback — manual `HarnessApproval` stage, `Verify` step, `StageRollback` failure strategy + `infra/scripts/rollback.sh`
- `cc:完了` Per-env infra — `infra/compose/{staging,prod}.yml` (registry images, prod hardening), `env.{staging,prod}.example`, `infra/scripts/{deploy,smoke,rollback}.sh`, `make ci` gate. **Self-migrating api** (alembic-on-startup + bootstrap admin) so fresh deploys come up with schema. *Verified end-to-end: `deploy.sh staging` → auto-migrate → bootstrap → smoke green.*
- `cc:完了` Strict-mypy cleanup — 91→0 errors (`py.typed` markers, typed generics/signatures, lazy-client `Any`, generator/union guards); `mypy --strict` now **blocking** in `make typecheck` (48 source files clean, tests excluded).
- `cc:完了` eslint flat-config — `apps/web/eslint.config.mjs` (next/core-web-vitals via FlatCompat, eslint 9); fixed internal nav to `next/link`; web lint now **blocking**. CI lint+typecheck gate (ruff + eslint + tsc + mypy-strict) is fully blocking.

---

## Open ADR follow-ups (PRD2 §15 — decide before/within the relevant epic)

- `cc:TODO` Vector-DB backend — Qdrant vs pgvector (E6, P0 benchmark)
- `cc:TODO` Embedding model — KO/EN/GB10 constraints (E6)
- `cc:TODO` Model routing — rule-based v1; LLM-judged routing experiment in Phase 2 (E7)
- `cc:TODO` UI graph library — React Flow / Cytoscape.js / Sigma.js / ECharts Graph (E10)

> Accepted ADRs 0001–0010: `.claude/memory/decisions.md`. Scaffold session retrospective +
> any new patterns: `.claude/memory/{session-log,patterns}.md`.
